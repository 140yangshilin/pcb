<template>
    <div class="bg-white p-6 rounded shadow">
        <h2 class="text-lg font-semibold mb-4">PCB工艺</h2>
        <p class="text-gray-500 text-sm">（后续添加字段）</p>
        <!--<label class="text-sm text-gray-600">{{ t(field.label) }}</label>
        <select v-if="field.type === 'select'" v-model="props.modelValue[field.id]"
            class="border px-2 py-1 rounded w-full">
            <option v-for="opt in field.options" :key="opt.value" :value="opt.value">
                {{ t(opt.label) }}
            </option>
        </select>
        <input v-else-if="field.type === 'input'" type="text" v-model="props.modelValue[field.id]"
            class="border px-2 py-1 rounded w-full" />-->
    </div>
</template>

<script setup>
// 占位组件，等你添加字段后填充
</script>